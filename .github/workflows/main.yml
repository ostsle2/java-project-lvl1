name: Java CI

on:
  - push

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up openJDK
        uses: actions/setup-java@v1
        with:
          java-version: '15'
      - name: Build with Gradle
        run: make build
